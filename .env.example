# Libby Downloader Configuration
# Copy this file to .env and customize as needed

# ============================================
# Session & Browser Configuration
# ============================================

# Base directory for all Libby data (cookies, browser data, etc.)
# Default: ~/.libby-downloader
# LIBBY_DATA_DIR=/path/to/libby/data

# Path to cookies.json file
# Default: ~/.libby-downloader/cookies.json
# LIBBY_COOKIES_PATH=/path/to/cookies.json

# Browser user data directory (for session persistence)
# Default: ~/.libby-downloader/browser-data
# LIBBY_USER_DATA_DIR=/path/to/browser/data

# Run browser in headless mode (no visible window)
# Default: false
# LIBBY_HEADLESS=true

# Browser timeout in milliseconds
# Default: 60000 (60 seconds)
# LIBBY_BROWSER_TIMEOUT=60000

# ============================================
# Download Configuration
# ============================================

# Output directory for downloaded audiobooks
# Default: ~/Downloads/Libby
# LIBBY_OUTPUT_DIR=/path/to/output

# Temporary directory for download processing
# Default: system temp dir (/tmp/libby-downloader)
# LIBBY_TEMP_DIR=/path/to/temp

# Keep temporary files after processing (for debugging)
# Default: false
# LIBBY_KEEP_TEMP=false

# Merge chapters into a single file
# Default: true
# LIBBY_MERGE=true

# Embed metadata (cover art, title, author, etc.)
# Default: true
# LIBBY_METADATA=true

# ============================================
# Logging Configuration
# ============================================

# Log level: debug, info, warn, error
# Default: info
# LIBBY_LOG_LEVEL=info

# Enable verbose logging (same as --verbose flag)
# Default: false
# LIBBY_VERBOSE=false

# ============================================
# Notes
# ============================================

# - Environment variables override default values
# - CLI arguments override both environment variables and config files
# - Priority: CLI > ENV > Config Files > Defaults
# - Stealth mode settings (safe/balanced/aggressive) are in config/stealth.json
