/**
 * Interactive CLI - Now handled by Ink components in src/ui/ink/InteractiveMenu.tsx
 * This file is kept for backward compatibility with tests.
 */

export async function runInteractive(_dataDir?: string): Promise<void> {
  // Interactive mode is now rendered via Ink in cli.ts
  // This function is no longer called directly
}
